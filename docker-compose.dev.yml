services:
  backend:
    command: npm run dev

  proxy:
    volumes:
      - ./proxy:/app
      - /app/node_modules
    command: npm run dev

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: builder
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5274:5173"
    command: npm run dev -- --host

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ./ollama_data:/root/.ollama
    networks:
      - sandbox-net
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 8g
    restart: unless-stopped

  auto-complete:
    image: imageName
